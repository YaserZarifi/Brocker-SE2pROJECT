# BourseChain - Sprint Retrospective

**پروژه**: سامانه کارگزاری بورس آنلاین  
**دانشگاه**: امیرکبیر - درس مهندسی نرم‌افزار ۲  
**تاریخ**: بهمن ۱۴۰۴  

---

## What Went Well ✅

1. **Modular Monolith**: جداسازی منطقی با Django apps باعث شد هر بخش مستقل توسعه و تست شود.
2. **تست‌های جامع**: 163 تست (150 Django + 13 Hardhat) اطمینان از عدم regression داد.
3. **Non-blocking blockchain**: طراحی به گونه‌ای که بلاکچین هرگز matching را block نکند.
4. **Docker از ابتدا**: راه‌اندازی با یک دستور برای ارائه و demo.
5. **مستندات CONTEXT.md**: یک منبع مرکزی برای فهمیدن کل پروژه.

---

## What Could Be Improved 🔄

1. **Docker از روز اول**: اگر از Sprint 1 docker-compose داشتیم، مشکلات CRLF و migration زودتر برطرف می‌شدند.
2. **Mirror از ابتدا**: اگر از همان اول image ها از آروان می‌بودند، وقت کمتری برای رفع 403 صرف می‌شد.
3. **.gitattributes**: برای فایل‌های .sh از اول `eol=lf` تنظیم می‌شد.
4. **Contract deploy در Docker**: setup service به artifacts در hardhat container دسترسی ندارد؛ باید volume یا shared build در نظر گرفته شود.

---

## Action Items برای پروژه‌های بعدی

1. **.gitattributes** اضافه شود:
   ```
   *.sh text eol=lf
   ```

2. **CI/CD**: GitHub Actions برای:
   - اجرای تست‌ها
   - بررسی line ending
   - Build Docker (اختیاری)

3. **Health check ساده‌تر**: برای Hardhat از Node.js استفاده شد چون `nc` در alpine نبود؛ در image های بعدی `nc` نصب شود یا از curl استفاده شود.

4. **مستندسازی مشکل Docker Hub**: در README برای کاربران ایران راهنمای mirror قرار گیرد.

---

## یادگیری‌های کلیدی

- **CRLF vs LF**: در multi-platform development همیشه line ending را چک کنید.
- **Migration race**: فقط یک سرویس migration بزند؛ بقیه منتظر healthy شدن آن بمانند.
- **Docker در ایران**: Mirror های داخلی ضروری هستند.
- **Health check**: انتخاب ابزار مناسب (nc، curl، node) بسته به base image.

---

## جمع‌بندی

پروژه با موفقیت به پایان رسید. چالش‌های فنی شناسایی و حل شدند. تجربیات در Incident Postmortem ها و این Retrospective ثبت شدند.
